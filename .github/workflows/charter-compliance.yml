name: Charter Compliance Check Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Charter Compliance Check Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled status
        run: echo "Charter Compliance Check is disabled while Cahootz governance docs are reset."
